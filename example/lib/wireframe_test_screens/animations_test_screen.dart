import 'package:flutter/material.dart';

class AnimationsTestScreen extends StatelessWidget {
  const AnimationsTestScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
